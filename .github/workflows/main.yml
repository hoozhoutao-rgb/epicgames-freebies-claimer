Name: Daily Claim (Stable)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 16 * * *"

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 使用 Node 20 (解决 File 报错)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 🔴 关键修改：不要用 npm ci，要用强制重装
      - name: Install dependencies
        run: |
          rm -rf node_modules
          rm -f package-lock.json
          npm install

      # 使用 npm start (解决找不到文件报错)
      - name: Run daily task
        env:
          EPIC_GAMES_DATA: ${{ secrets.EPIC_GAMES_DATA }}
          NODE_ENV: production
        run: npm start
